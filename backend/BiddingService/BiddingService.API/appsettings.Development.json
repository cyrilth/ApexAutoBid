{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "MongoDbConnection": "mongodb://mongoadmin:mongopw@localhost:27017/?directConnection=true"
  },
  "IdentityServiceUrl": "https://localhost:5001",
  "RabbitMq": {
    "Host": "localhost",
    "Username": "guest",
    "Password": "guest"
  },
  // Phase 5 Tasks 6/7 — dev address of AuctionService.API's dedicated gRPC Kestrel endpoint
  // (that service's appsettings.Development.json; cleartext HTTP/2, see
  // GrpcFallbackAuctionProvider/Program.cs's AppContext.SetSwitch remarks). Container/k8s
  // addressing (a service DNS name instead of localhost) is a later Phase 8/9 concern, same as
  // every other *ServiceUrl/cluster address in this repo today.
  "Grpc": {
    "AuctionServiceUrl": "http://localhost:7054"
  }
}
