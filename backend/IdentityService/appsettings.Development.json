{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5434;Database=apexautobid_identity;Username=postgres;Password=postgrespw"
  },
  // Phase 11 Task 2 — this service's own base URL, used two ways: (1) the admin API's JwtBearer
  // Authority (HostingExtensions.cs) — this service validates tokens against itself, since it IS
  // the IdentityServer; (2) the admin API's own OpenAPI document's OAuth2 security scheme
  // (OpenApi/OAuth2SecuritySchemeTransformer.cs), matching every other service's identical
  // "IdentityServiceUrl" config key (AuctionService.API/etc. appsettings.Development.json).
  "IdentityServiceUrl": "https://localhost:5001",
  "Smtp": {
    "Host": "localhost",
    "Port": 1025,
    "UseSsl": false,
    "FromAddress": "noreply@apexautobid.local",
    "FromName": "ApexAutoBid (Dev)"
  },
  // Cloudflare's own official always-pass Turnstile test keys — published specifically for dev/
  // CI/e2e use and safe to commit (Requirements.md §3.4/CLAUDE.md §6, unlike Google's
  // credentials): https://developers.cloudflare.com/turnstile/troubleshooting/testing/
  "Turnstile": {
    "SiteKey": "1x00000000000000000000AA",
    "SecretKey": "1x0000000000000000000000000000000AA"
  }
}
